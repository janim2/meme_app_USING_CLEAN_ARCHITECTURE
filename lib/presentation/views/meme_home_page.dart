import 'package:flutter/material.dart';

class MemeAppHome extends StatelessWidget {
  const MemeAppHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Align(alignment: Alignment.center, child: Container()));
  }
}
